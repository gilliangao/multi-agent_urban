# Frontend

This frontend is a Streamlit-based control room for live multi-agent planning sessions.

Run it from the repository root:

```bash
streamlit run frontend/app.py
```

It uses the backend Python package from `backend/src/urban_forest_ai`.

Planned future upgrade path:

- React or Next.js interface
- real-time event streaming from the backend API
- richer spatial visualization
