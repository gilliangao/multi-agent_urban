# Prompt Assets

This folder is reserved for prompt assets and agent instructions as the project grows.

Planned contents:

- reusable agent system prompts
- judge templates
- experiment-specific prompt variants
- ablation prompt sets for evaluation
